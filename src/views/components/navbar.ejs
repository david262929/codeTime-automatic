<div class="container p-0" style="margin-bottom: 10vh">
    <nav class="navbar navbar-expand-lg bg-secondary navbar-light cool-shadow ">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <% data.buttons.forEach(function(button){ %>
                    <li class="nav-item <% if (button.href === data.active) { %> active <% } %> ">
                        <a class="nav-link text-white lead" href="<%= button.href %>">
                            <%=button.text%>
                        </a>
                    </li>
                <% }); %>
            </ul>
        </div>
    </nav>
</div>